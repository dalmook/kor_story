# 제작 에이전트 규칙

## 범위
이 저장소는 한국풍 수묵담채 쇼츠 제작 명령서다. 사용자 요청 없이 다른 프로젝트, Hermes 설정, 인증, 모델 제공자, 기존 편집 환경을 변경하지 않는다. 현재 작업은 사용자가 지정한 이야기 하나다. 번호 미지정 시 유료/크레딧 작업을 시작하지 말고 명령 선택 문서를 보여준다.

## 반드시 읽을 문서
1. HERMES_COMMANDS.md
2. docs/MASTER_PRODUCTION.md
3. docs/INK_EDITING_SPEC.md
4. 선택한 stories/*.md
5. docs/SOURCES.md

## 절대 조건
- 한 편은 Flow 독립 생성 7컷, 각 4초. 최종 28초, 9:16, 30fps, 840프레임.
- Flow 지출 한도는 편당 50크레딧. 720p/4초/x1/7크레딧 조건이 실제 화면에서 확인될 때만 7회 최초 생성, 예상 49크레딧.
- 출력 개수 x2, 자동 재시도, 영상 Edit/Extend, 유료 업스케일, 추가 결제, 다른 유료 API 사용 금지.
- 5편을 한도 50으로 만들 수 있다고 보고하지 않는다. 모든 편을 자동으로 이어서 생성하지 않는다.
- 이미지 7장을 먼저 확보·검수한다. 편집으로 이미지 줌만 넣어 Flow 영상 7개를 대체하지 않는다.
- 시작 이미지 1장 → 해당 컷의 4초 영상 1개. 서로 다른 컷을 시작/끝 프레임으로 강제 연결하지 않는다.
- Flow는 인물/사물의 작은 동작만. 먹 전환·자막·변신 은유·음향은 로컬 후반 편집.
- 전환은 컷 경계 위의 가림막 오버레이. 7×120프레임을 겹쳐 영상이 짧아지는 오류 금지.
- 내레이션은 동일 화자 1개의 연속 음성으로 제작. 컷·자막마다 별도 화자처럼 읽게 만들지 않는다.
- 원작 소개와 창작을 구분한다. 03과 05는 창작 설화이며 실제 채록 이야기처럼 소개하지 않는다.
- 인증 정보·쿠키·개인정보·음원 원본·대용량 생성 미디어는 Git에 올리지 않는다. 서비스 워터마크 제거를 지시하지 않는다.
- 로그인/CAPTCHA/연동 부재를 우회하지 않는다. 그런 경우 prompts, asset 목록, 수동 생성 체크리스트까지 완성하고 NEEDS_FLOW_INPUT 상태로 정직하게 보고한다.
- 기존 무료/구독 포함 이미지·TTS 환경을 우선 사용한다. 추가 과금이 필요한 기능은 호출하지 않는다.
- 실제 MP4를 렌더·검수하지 않았다면 영상 완료라고 보고하지 않는다. 자동 지표와 사람의 시청 검수도 구분한다.

## 역할
Hermes: 감독, 캐릭터/이미지 일관성, 비용 기록, Flow 생성 운영, 실제 결과 검수.
Codex CLI: 로컬 편집 프로젝트, 타임라인, 결정론적 수묵 효과, 음성/자막, 렌더, 기술 검증.

기존 환경에서 사용 가능한 영상·자막·렌더 스킬을 확인하고 해당 SKILL.md를 읽는다. 설치되어 있지 않은 도구를 설치되어 있다고 가정하거나 미확인 CLI 옵션을 만들지 않는다. 큰 영상 생성·업스케일 모델은 새로 설치하지 않는다.
