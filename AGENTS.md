# 먹빛설화 제작 규칙 — v2 / 내레이션 전용 · 한국어 + 영어

## 사용자 최신 요구 / 최우선
- 등장인물 대사, 직접 인용, 역할극, 립싱크를 사용하지 않는다. 기도와 약속도 내레이터가 3인칭으로 설명한다.
- 한 이야기마다 한국어판과 영어판을 둘 다 만든다. 각 버전은 해당 언어의 내레이션 1명 + 해당 언어 자막만 사용한다. 한국어/영어 동시 낭독 또는 2개 국어 자막을 한 화면에 넣지 않는다.
- Flow 원본은 언어 공용으로 딱 7컷, 각 4초만 생성한다. 언어별 7컷씩 총 14컷 생성 금지. 7개 원본으로 무음·무자막 공용 마스터를 한 번 만든 뒤 음성/자막을 교체하여 두 버전으로 로컬 렌더한다.
- 한 이야기의 KO+EN 합산 Flow 예산 최대 50. 실제 UI가 720p/4초/x1/7크레딧일 때 최초 생성 7회 = 예상 49. 49는 영상 생성 예산이지 TTS까지 포함한 전체 제작비 보장이 아니다.
- 영어판을 위해 Flow에 번역/더빙/오디오 편집을 요청하지 않는다. 자동 재시도, x2 출력, 유료 Edit/Extend/Upscale, 추가 결제 금지.

## 읽기 순서
AGENTS.md → docs/MASTER_PRODUCTION.md → docs/BILINGUAL_NARRATION.md → docs/INK_EDITING_SPEC.md → 선택 stories/*.md → docs/SOURCES.md.

## 역할과 범위
Hermes는 감독·이미지 일관성·비용/Flow 운영, Codex CLI는 공용 타임라인·전환·KO/EN 오디오/자막·검증 담당이다. 선택한 이야기 하나만 실행하고 다른 편을 연속 생성하지 않는다. 번호가 없으면 크레딧을 쓰지 않고 준비 단계까지만 진행한다.

현재 저장소는 명령서/텍스트 대본이다. 실제 영상·음성·편집기가 구현되었다고 가정하지 않는다. 기존 프로젝트·미커밋 파일·인증·모델 설정을 보존한다. 기존 환경과 설치된 관련 SKILL.md를 확인하여 재사용하고 큰 영상 생성/업스케일 모델을 새로 설치하지 않는다.

## 이미지와 영상
몽환적인 한국풍 한지 수묵담채화. 먼저 이미지 7장을 확정하고 시작 프레임 1장 → 독립된 4초 영상 1개로 진행한다. 언어별 이미지도 다시 만들지 않는다. 인물/사물 자체가 움직여야 하며 정지 이미지 줌으로 대체하여 성공 처리하지 않는다. Flow 프롬프트에는 대본을 넣지 않는다. 모든 생성 원본 오디오는 로컬에서 제거한다. 음소거만으로 화면 속 말하는 입이 해결되지는 않으므로 립싱크처럼 보이는 움직임도 검수한다.

## 시간과 현지화
두 버전 모두 28.000초/30fps/840프레임. 7컷 × 120프레임과 전환·카메라·색은 동일하다. 영어가 길다고 영어 영상만 늘리거나 컷을 더 생성하지 않는다. 문장을 줄이고 내레이션을 다시 만든다. 언어별로 실제 음성을 듣고 각각 타임스탬프를 만든다. 한글 SRT 시간을 복사해 영어로 번역하는 방식 금지.

## 지출과 재개
영상+이미지 등 같은 Flow 잔액을 쓰는 작업을 모두 원장에 기록한다. 이미지 비용으로 49 예약분을 침범하지 않는다. TTS/음원은 추가 과금 없는 기존 환경을 우선하고 별도 비용이 필요하면 호출 전에 멈춘다. 금전적 잔액과 Flow 크레딧을 혼동하지 않는다.

작업 ID와 클립 SHA-256을 저장한다. 이미 생성된 클립은 재개/영어판 생성 시 재사용한다. 브라우저 타임아웃은 재제출 근거가 아니다. 실패/환불은 실제 상태로 기록한다. 로그인/연동이 없으면 준비 파일과 수동 체크리스트를 끝내고 NEEDS_FLOW_INPUT으로 보고한다. 우회·가짜 API 금지.

## 완료 조건
두 최종 MP4, 두 내레이션 WAV, 두 SRT, 공용 무음 마스터, 비용원장, 두 언어의 검수 기록이 있어야 한다. 한 언어만 있으면 PARTIAL_LOCALIZATION, 실제 청취/시청이 없으면 HUMAN_REVIEW_REQUIRED. 자동 검수 PASS를 자연스러운 음성/좋은 스토리의 보증으로 말하지 않는다.

03/05는 창작 설화, 01/02/04는 축약 각색이다. 영상의 분류 표시는 언어별 후반 오버레이로만 추가한다. 비밀정보·쿠키·생성 미디어를 Git에 올리지 않는다. 서비스 표시를 제거하지 않는다. 업로드/예약 발행/추가 결제는 별도 요청 없이 하지 않는다.
