# 먹빛설화 — Codex direct / v4

## 현재 실행 계약

사용자는 Hermes를 제외하고 Codex가 이미지·Flow·음성·음향·편집·검수까지 직접 수행하도록 변경했다. 이 문서와 `production/*_v4.json`이 실행 기준이다. 과거 문서의 Hermes 위임, 최종28초/840프레임, 모든 경계의 전면 가림막은 폐기했다.

완성본은 **오프닝2초 + 본문7×4초 + 엔딩/2부예고3초 = 33초/990프레임**이다. 본문만28초/840프레임이다. 원화는9장, Flow 본문 영상은7개. 앞뒤 이미지에는 로컬의 아주 작은 움직임만 사용한다. 본문의 실제 움직이는 영상7개를 정지 이미지로 대체하지 않는다.

KO/EN은 동일한 시각 마스터를 사용한다. 인물대사·립싱크 없이 언어별 내레이터 한 명의 연속 음성을 제작한다. 실제 음성에 각각 자막을 정렬한다. 언어 때문에 영상이나 원화를 다시 생성하지 않는다.

## 반드시 읽기

START_CODEX.md → docs/VIDEO_AUDIT_V4.md → docs/RESEARCH_V4.md → docs/DESIGN_BIBLE_V4.md → docs/CODEX_WORKFLOW_V4.md → production/director_v4.json → production/catalog_v4.json → 선택한 상세 override 또는 기존 story/episode의 내용·캐릭터·모션 블록.

기존 `narration/manifest.json`과 `episodes/`, `stories/`는 본문 내용 참조다. 그 안의28초·옛 전환·금지된 앞뒤카드 규칙은 v4를 덮어쓰지 않는다. 특히 첫 대상의 원고/구도는 `production/01_sun_and_moon_p01_v4.json`이 우선한다. 기존 원고 해시를 억지로 바꾸지 말고 실행용 TXT는 run에 생성한다. archive는 역사 기록일 뿐 실행하지 않는다.

## 디자인 잠금

원본1080×1920 기준 제목KO108px/EN92px, 자막KO72px/EN68px, 자막최소64px. 화면 미리보기 배율을 폰트값에 다시 곱하지 않는다. 자막x96..924/y1296..1472, 최대2줄, 최소1.8초 노출, 단어별 점멸/가라오케/바운스 금지. 실제 폰트의 glyph bounds를 검사하고 모바일360px 미리보기를 본다.

전환은 cut 또는8프레임 중심 디졸브(최대10)만. 본문 디졸브는 최대2곳. 검은 먹덩어리/다각형/밝은 전면가림막 금지. 한지를 살아 있게 표현하는 것은 그림/안개/옷의 움직임이지 화면 덮기 효과가 아니다. 전체 영상·음성 배속 금지. BGM/SFX를 누락하고 완성으로 보고하지 않는다.

## 예산·도구·안전

기존 깨끗한 원본부터 찾아 재사용한다. 자막이 박힌 완성MP4는 클린원본이 아니다. 서비스 표시를 지우거나 사용자 파일을 덮어쓰지 않는다. 기존 미커밋 변경과 원본을 보존한다.

도구를 실제 발견·확인한 뒤 사용한다. Codex 네이티브 이미지 도구와 승인된 브라우저/컴퓨터제어를 우선하고 기능명·API·로그인 상태를 지어내지 않는다. 문서의 프롬프트를 만드는 것으로 실제생성을 대신하지 않는다. 이미지/TTS가 포함된 사용인지 확인하며 추가 현금 지출 승인한도는0이다.

Flow 실제720p/4초/x1/7크레딧 조건이면 신규7개 예상49, episode 한도50. 이미지 등 같은 잔액에서 차감한 작업도 합산한다. x2·자동 유료재시도·유료Edit/Extend/Upscale·영어더빙 재생성 금지. 첫클립 품질 확인 후 나머지를 순차 진행한다. 기존 실패/미확정작업은 원장에 반영하고 run 이름 변경으로 예산을 초기화하지 않는다.

인증/도구 부재는 WAIT_FOR_LOGIN 또는 BLOCKED_TOOL_ACCESS, 추가과금 필요는 WAIT_FOR_BUDGET_APPROVAL. 로그인/CAPTCHA/사용량제한을 우회하지 않는다. 로컬 편집 교정은 최대3회, 유료재생성과 구분한다. 검수 없이 성공이라 말하지 않는다. 모델 첫시도 품질은 보장하지 않으며 사전검수로 실패를 줄인다.

## 완료와 게시

최종33초 두MP4, 무음·무문자 마스터, 원화9장, 원본7개, 언어별WAV/SRT/실측정렬, 배경음·효과음 출처, 원장, 해시, 실제영상/음성/모바일검수 증거가 필요하다. 준비스크립트 PASS는 미디어 PASS가 아니다. 실제로 듣지 못하면 LISTENING_REVIEW_PENDING. 완료보고는 확인한 범위만 READY_FOR_USER_REVIEW로 구분한다.

기존 youtube 문구는 보존하되 실행용 설명은33초로 내보낸다. 1부예고이미지에는 다음 장면 일부를 보여도 본문의 결말을 먼저 폭로하지 않는다.

요청이 GitHub 문서수정이면 그 작업만 수행한다. **사용자가 START_CODEX의 제작 실행을 요청하면 선택한 episode의 허용된 예산·도구 범위 안에서 실제 이미지/Flow/음성/렌더를 진행한다.** 추가 현금결제, 다음2부 자동실행, 유튜브게시·예약·채널변경은 그 제작요청에 포함되지 않는다.

## 시리즈 패키징 추가 계약 — series-packaging-1

추가로 `docs/SERIES_PACKAGING_V4.md`와 `production/series_packaging_v4.json`을 읽는다. 기술 규격은 위 v4를 유지하며, 표지 제목·오프닝 구도·편별 시각적 초점은 이 새 오버레이가 우선한다. `codex_production.py check/prepare`가 자동으로 합치므로 렌더는 해당 run의 `run_plan.json.cards`와 `packaging/brief.json`을 사용한다. 1부 예고 제목은 2부의 새 제목과 맞춘다. 해와 달2부는 반드시 기존 `production/01_sun_and_moon_p02_v4.json` 상세안을 로드한다.

각 편은 중심 대상/인물군 하나와 이야기 단서 하나로 첫 프레임부터 질문을 만든다. 빈 로고나 추가 VO는 넣지 않는다. 포스터용 표지는 기존 `images/00_opening.png`에 언어별 제목을 로컬 합성하여 파생한다. 추가 원화/Flow/현금지출은 없다. 핵심 원고·Flow 모션·KO/EN 공용 마스터를 보존하고, 질문 메모를 내레이션에 더하지 않는다. 기존 승인 이미지와 연속성 기록이 새 구도 제안보다 우선한다.

7컷 초점은 새 사건이나 행동을 추가하는 지시가 아니다. 한국 수묵담채·복식·건축·승인 색을 유지하고 다른 제작자 원화·캐릭터·로고를 복제하지 않는다. 선정적/잔혹 괴담으로 장르를 바꾸거나 스크린샷의 좋아요만으로 성공 공식을 확정하지 않는다. `packaging_review.json`은 PENDING으로 시작하며 실제 증거 없이 PASS 처리하지 않는다. 이번 패키징 변경만으로 기존 자산을 자동 재생성하거나 게시하지 않는다.
